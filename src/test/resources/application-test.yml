spring:
  datasource:
    url: jdbc:h2://mem:YKB_HR_DB;DB_CLOSE_DELAY=1
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: create-drop
        formatSql: true


